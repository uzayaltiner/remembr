#!/usr/bin/env node
// Entry point for the published `remembr` CLI.
//
// After `npm run build`, `dist/cli.js` is the compiled entry. We re-export
// it here so that `npm i -g remembr` exposes the binary at the symlink
// without needing a build step on the user's machine.

import '../dist/cli.js';
